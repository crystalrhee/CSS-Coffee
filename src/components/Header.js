import React from 'react';
import './Header.scss';

function Header() {
    return (
        <header>
            <a href="/">CSS Coffee</a>
            <a href="/">JS Coffee</a>
        </header>
    );
}

export default Header;